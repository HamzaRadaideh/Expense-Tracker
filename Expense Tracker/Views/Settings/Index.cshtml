@{
    ViewData["Title"] = "Settings";
}

<h2>Settings</h2>

<div class="settings-options">
    <a class="btn btn-primary mb-3" href="@Url.Action("ChangeName", "Settings")">Change Name</a>
    <a class="btn btn-primary mb-3" href="@Url.Action("ChangeEmail", "Settings")">Change Email</a>
    <a class="btn btn-primary mb-3" href="@Url.Action("ChangePassword", "Settings")">Change Password</a>
    <a class="btn btn-primary mb-3" href="@Url.Action("ChangeTheme", "Settings")">Change Theme</a>
</div>
